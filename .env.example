# IcarusEye v2 — copy to .env, never commit .env

REDIS_HOST=redis
REDIS_PORT=6379

# ── HLS video source ────────────────────────────────────────────────────────
# This is the path INSIDE the container that ffmpeg reads from.
#
# Option A — local file (dev):
#   1. Create a sample/ folder next to docker-compose.yml on your host
#   2. Drop any .mp4 in it: sample/myvideo.mp4
#   3. It mounts to /app/sample/ inside the container
#   HLS_SOURCE=/app/sample/myvideo.mp4
#
# Option B — RTSP stream (IP camera or Jetson relay):
#   HLS_SOURCE=rtsp://192.168.1.x:554/stream
#
# Option C — RTP (from relay service once built):
#   HLS_SOURCE=rtp://127.0.0.1:5000
#
HLS_SOURCE=/app/sample/sample.mp4

# Where ffmpeg writes HLS segments (inside container, no need to change)
HLS_SEGMENT_DIR=/tmp/icaruseye_hls
