# Pipeline dependencies for CPU / cross-platform mode (no TensorRT)
# Used by docker/pipeline/Dockerfile.cpu
# Supports PyTorchEngine (.pt models) on CPU, MPS (Apple Silicon), or CUDA.
numpy<2
opencv-python-headless>=4.9.0
ultralytics>=8.3
